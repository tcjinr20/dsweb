#!/usr/bin/env python
# coding:utf-8

import multiprocessing
import time
import psutil
import threading

class Dt(threading.Thread):
    def

def getCPUstate(interval=1):
    p = psutil.cpu_percent(interval)
    phymem = psutil.virtual_memory()
    p,phymem.percent

def func(status):
    rm = 0;
    while(status):
        print rm;
        rm+=1;

if __name__ == "__main__":

